{
  "name": "running-stats",
  "version": "0.0.0",
  "description": "Calculate running statistics from a stream of data. Includes directional statistics, for circular values (degrees).",
  "keywords":["statistics","directional statistics"],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Hiatt, sam.hiatt@weather.com",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^1.4.1"
  }
}
